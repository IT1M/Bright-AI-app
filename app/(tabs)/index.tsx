// Import the newly created HomeScreen component
import HomeScreenComponent from '../../src/HomeScreen.js';

export default function HomeScreen() {
  // Render the imported HomeScreen component
  return <HomeScreenComponent />;
}

// Styles are no longer needed here as they are defined within HomeScreen.js
